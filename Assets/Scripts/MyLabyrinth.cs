using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MyLabyrinth : MonoBehaviour
{
    byte firstPosition;

    public MyLabyrinth(byte position)
    {
        firstPosition = position;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
